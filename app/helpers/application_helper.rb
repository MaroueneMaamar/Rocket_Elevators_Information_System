module ApplicationHelper
    # def current_user
    #     @current_user ||= User.find_by(id: session[:user_id])
    # end

    # before_action :set_current_user

    # def set_current_user
    #     Quote.current_user = current_user
    # end
end
